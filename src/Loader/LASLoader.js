class LASLoader {
    /**
     * @param {ArrayBuffer} data - The file content to parse
     * @param {Object} options - Options to give to the parser
     * @param {Object} options.las - TODO
     * @param {number?} options.las.skip - TODO
     * @param {number|string} optons.las.colorDepth - TODO
     */
    constructor(data, options) {
        const lf = undefined;
    }
}

class LASHandler {
    constructor() {
        this.node = '';
    }

    push(lasBuffer) {
    }
}
